@page
@model Ceramics.Pages.Warehouses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Warehouses</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseLocation[0].ChemID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseLocation[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WarehouseLocation[0].WarehouseLocationID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.WarehouseLocation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ChemID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WarehouseLocationID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
